apiVersion: v1
kind: Service
metadata:
  name: django-todo-service
spec:
  type: NodePort
  selector:
    app: django-todo-pod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 31000
